@using PetsWebService.Common.Model;

@model List<PetDetails>
@{
    ViewBag.Title = "Home Page";
}

<div>
    <table>
        @foreach (var element in Model)
        {
            <tr>
                <td>
                    <b>  @element.Gender.ToUpper() </b>
                    <table>
                        @foreach (var petname in @element.Pets)
                        {
                            <tr>
                                <td colspan="5"></td>
                                <td>
                                    @petname.ToString()
                                </td>
                            </tr>
                        }
                    </table>
                </td>
            </tr>
        }
    </table>
</div>